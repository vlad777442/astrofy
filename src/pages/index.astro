---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";
import createSlug from "../lib/createSlug"
import profile from "../profile";

const posts = (await getCollection("blog")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const last_posts = posts.slice(0, 3);
---

<BaseLayout sideBarActiveItemID="home">
  <div class="pb-10 mt-5">
    <div class="text-xl py-1">Hey there 👋</div>
    <div class="text-5xl font-bold">I'm {profile.name}</div>
    <div class="text-2xl py-3 font-semibold">{profile.roles.join(" · ")}</div>
    <div class="py-2 max-w-3xl">
      <p class="text-lg">{profile.headline}</p>
    </div>
    <div class="mt-6 flex flex-wrap gap-3">
      {profile.links.resume && <a class="btn" href={profile.links.resume} target="_blank">Download Resume</a>}
      <a class="btn btn-outline" href="/cv">View Resume</a>
      {profile.links.linkedin && <a class="btn btn-outline" href={profile.links.linkedin} target="_blank">LinkedIn</a>}
      {profile.links.github && <a class="btn btn-outline" href={profile.links.github} target="_blank">GitHub</a>}
    </div>
  </div>

  <div>
    <div class="text-3xl w-full font-bold mb-2">My last projects {"</>"}</div>
  </div>

  <HorizontalCard
    title="Production ML System — Recsys CTR + Ranker"
    img="/post_img.webp"
    desc="End-to-end pipeline (PyTorch, Airflow, AWS) with online A/B testing; improved CTR by 7.3% over baseline."
    url="#"
    badge="ML"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Scalable Backend — Event-driven microservices"
    img="/post_img.webp"
    desc="Node.js/TypeScript services on Kafka + Postgres; 99.95% SLO, 2x throughput; full IaC and observability."
    url="#"
    badge="SWE"
  />
  <div class="divider my-0"></div>
  <HorizontalCard
    title="Research — Paper/Benchmark"
    img="/post_img.webp"
    desc="Reproducible experiments, SOTA baseline comparisons, and open-source code; accepted at [venue]."
    url="#"
    badge="Research"
  />

  <div>
  <div class="text-3xl w-full font-bold mb-5 mt-10">Latest writing</div>
  </div>

  {
    last_posts.map((post) => (
      <>
        <HorizontalCard
          title={post.data.title}
          img={post.data.heroImage}
          desc={post.data.description}
          url={"/blog/" + createSlug(post.data.title, post.slug)}
          target="_self"
          badge={post.data.badge}
        />
        <div class="divider my-0" />
      </>
    ))
  }
</BaseLayout>
